language: C++
os: freebsd
script:
  - cmake -S . -B build
  - cmake --build build
  - ctest --test-dir build
addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake
